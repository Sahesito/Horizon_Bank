'use server';

export const signIn = async () => {
    try{
        //Mutacion / Database / Fecth
    } catch(error) {
        console.error('Error', error)
    } 
}

export const signUp= async ( userData: SignUpParams ) => {
    try{
        //Crear una cuenta
    } catch(error) {
        console.error('Error', error)
    } 
}